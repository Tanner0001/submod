pataut_pop_high = { #Paternal Autocrat/Zhang Clique - Pop High
	paternal_autocrat > 0.28
	paternal_autocrat < 0.45
}

FNG_JAP_hostile = {
	has_global_flag = FNG_relations_JAP_broke
}

FNG_mantetsu_level_very_high = { #equivalent to informal_empire_5
	custom_override_tooltip = {
		tooltip = FNG_mantetsu_level_very_high_tooltip
		NOT = { check_variable = { FNG.FNG_JAP_influence_variable < 0.75 } }
	}
}

FNG_mantetsu_level_high = { #equivalent to informal_empire_4
	custom_override_tooltip = {
		tooltip = FNG_mantetsu_level_high_tooltip
		check_variable = { FNG.FNG_JAP_influence_variable < 0.75 }
		NOT = { check_variable = { FNG.FNG_JAP_influence_variable < 0.50 } }
	}
}

FNG_mantetsu_level_medium = { #equivalent to informal_empire_3
	custom_override_tooltip = {
		tooltip = FNG_mantetsu_level_medium_tooltip
		check_variable = { FNG.FNG_JAP_influence_variable < 0.50 }
		NOT = { check_variable = { FNG.FNG_JAP_influence_variable < 0.25 } }
	}
}

FNG_mantetsu_level_low = { #equivalent to informal_empire_2
	custom_override_tooltip = {
		tooltip = FNG_mantetsu_level_low_tooltip
		check_variable = { FNG.FNG_JAP_influence_variable < 0.25 }
		NOT = { check_variable = { FNG.FNG_JAP_influence_variable < 0.1 } }
	}
}

FNG_mantetsu_level_none = { #equivalent to informal_empire_1
	custom_override_tooltip = {
		tooltip = FNG_mantetsu_level_none_tooltip
		check_variable = { FNG.FNG_JAP_influence_variable < 0.1 }
	}
}

FNG_has_united_china = {
	custom_override_tooltip = {
		tooltip = FNG_has_united_china
		FNG = { has_cosmetic_tag = united_china }
	}
}

FNG_JAP_influence_active = {
	FNG = {
		exists = yes
		FNG_JAP_hostile = no
	}
	JAP = {
		exists = yes
		NOT = { has_war_with = FNG }
	}
}

FNG_JAP_mission_complete_trigger = {
	608 = {
		controller = {
			OR = {
				tag = FNG
				is_subject_of = FNG
			}
		}
	}
	OR = {
		592 = { #Guangzhou
			controller = {
				OR = {
					tag = FNG
					is_subject_of = FNG
				}
			}
		}
		613 = { #Nanjing
			controller = {
				OR = {
					tag = FNG
					is_subject_of = FNG
				}
			}
		}
		650 = { #Chongqing
			controller = {
				OR = {
					tag = FNG
					is_subject_of = FNG
				}
			}
		}
		1048 = { #Wuhan
			controller = {
				OR = {
					tag = FNG
					is_subject_of = FNG
				}
			}
		}
		FNG = { num_of_controlled_states > 34 } #Fengtian Starts with 10 - just prevent FNG from snaking around or something lame like that. If they have ANQ/SHD/GXC kinda redundant since it will fire. Then again I guess you could just ignore Beijing but that would be super annoying to code around so well congrats you broke KR (one of a million ways)
		#Okay 19 was too low, gonna up it now to 34 just to be safe.
	}
}
